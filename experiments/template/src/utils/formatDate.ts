/**
 * formatDate — baseline (INTENTIONALLY BROKEN for the experiment task)
 *
 * Task: Fix this function so that formatDate('2026-03-12') returns 'Mar 12, 2026'.
 *
 * Current (wrong) implementation returns the ISO string unchanged.
 */
export function formatDate(isoDate: string): string {
  // BUG: just returns the raw ISO string — does NOT format it
  return isoDate;
}
